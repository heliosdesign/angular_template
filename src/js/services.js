'use strict';

angular.module('angularApp.services', [])

	// .factory('loadData', function( $q, $http ){
	// 	var defer = $q.defer()

	// 	$http.get('DATA')
	// 		.success(function(response){
	// 			defer.resolve(response.data)
	// 		})

	// 	return defer.promise
	// })
